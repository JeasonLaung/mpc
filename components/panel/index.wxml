<!--components/m-panel/m-panel.wxml-->
<view class="m-panel">
  <view class="m-panel__title">
    {{title}}
  </view>
  <view class="{{inner ? 'm-panel__inner' : ''}}">
    <slot/>
  </view>
  
</view>
<view class='m-note' wx:if="{{note}}">
  {{note}}
</view>
